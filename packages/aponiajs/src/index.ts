/**
 * The reserved public facade for AponiaJS.
 *
 * The package is private and unpublished until it re-exports a settled
 * framework API from `@aponiajs/common`, `@aponiajs/core`, and
 * `@aponiajs/platform-elysia`. It intentionally exports nothing today, so
 * nothing can depend on a surface that has not been designed yet.
 */
export {};
